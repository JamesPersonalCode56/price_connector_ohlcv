# Chapter 8 - Health and Metrics

Health endpoints:
- /health: liveness check
- /ready: readiness check with per-exchange status
- /metrics: Prometheus metrics

These endpoints are exposed on CONNECTOR_WSS_HEALTH_CHECK_PORT (default 8766).
